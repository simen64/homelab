apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: metallb
  namespace: flux-system
spec:
  interval: 10m
  targetNamespace: metallb
  chart:
    spec:
      chart: metallb
      version: '7.10.4'
      sourceRef:
        kind: HelmRepository
        name: metallb
        namespace: flux-system
      interval: 10m